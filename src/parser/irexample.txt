// types: ptr(...), funcptr(...), u8, i8, u16, i16, u32, i32, u64, i64, f32, f64
// composite types: structs, arrays

struct Vec2
{
    f32 x;
    f32 y;
}


f32 plus_one(f32 a)
{
    return a + 1.0f32;
}

funcptr(f32, (f32)) r_add_one()
{
    return plus_one;
}

f32 add(f32 a, f32 b)
{
    //u8[16] array1;
    //Vec2 vec;
    funcptr(f32, (f32)) f;
    //f = r_add_one();
    //return f(a + b);
    return (r_add_one())(a + b);
}
